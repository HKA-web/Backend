debug: true
allowed_hosts: ["localhost", "127.0.0.1"]
databases:
  default:
    ENGINE: django.db.backends.postgresql
    NAME: postgres
    USER: postgres
    PASSWORD: 111111
    HOST: localhost
    PORT: 5432
  sqlserver:
    server1:
      driver: "{SQL Server}"
      pipe: "\\\\127.0.0.1\\pipe\\sql\\query"
      database: "master"
      uid: "sa"
      pwd: ""